[
    "cudah found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit first",
    "cuda_runtime_apih found check path cuda toolkit first",
    "cudah found check path cuda toolkit fi"
]