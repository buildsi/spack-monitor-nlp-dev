[
    "base specifier must name class",
    "base specifier must name class",
    "base specifier must name class",
    "base specifier must name class",
    "base specifier must name class",
    "base specifier must name class",
    "base specifier must name class"
]